Adapted to wine-10.15 by Operit (downgraded: no timeline/timer fields in 10.15).
From: GE-Proton <git@localhost>
Subject: [PATCH] winepulse: Reset stream started state after an underflow.

diff --git a/dlls/winepulse.drv/pulse.c b/dlls/winepulse.drv/pulse.c
index 9a218e7..c6f5d89 100644
--- a/dlls/winepulse.drv/pulse.c
+++ b/dlls/winepulse.drv/pulse.c
@@ -381,6 +381,7 @@ static void pulse_underflow_callback(pa_stream *s, void *userdata)
     struct pulse_stream *stream = userdata;
     WARN("%p: Underflow\n", userdata);
     stream->just_underran = TRUE;
+    stream->started = FALSE;
 }
 
 static void pulse_started_callback(pa_stream *s, void *userdata)
 
